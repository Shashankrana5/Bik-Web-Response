export const DisplayTicketMessage = () => {
  return <div id="display-ticket-message">
    <div>

    this is the ticket messages.
    </div>
        {/* <div id="ticket-content">
        {ticketContent &&
          ticketContent.map((ticket) => {
            return (
              <div key={ticket._id}>
                <span>{ticket.senderName}</span>
                <span>{ticket.content}</span>
              </div>
            );
          })}
      </div> */}
  </div>;
};
